### Current bugs
* [FIXED] When in darkmode purple is hard to read (allow colour customization)
* Needs a toggle so it doesn't autoset per website, instead users can choose whether to enable it or not on a website
* [FIXED] POS highlighting colourblind friendly tags become part of the articles if using TTS or occasionally removing it, change this so its an overlay and not part of the text. "create" becomes "vcreate".
* [FIXED] Sentence splitting splits in the middle of sentences. Need it so although complexity may warrant splitting, it won't until the sentence has ended.
* [FIXED] Sentence splitting removes hyperlinks and certain other formatting. Should be handled the same as bolding, and POS highlighting
* Cannot hide menu upon click of the page, instead I need to click the icon again. Just annoying.
* [FIXED] Bold auto sets on some pages but does not align with what is showing up on toggle
