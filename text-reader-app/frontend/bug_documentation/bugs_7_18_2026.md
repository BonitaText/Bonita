### Current bugs
* [NOTBUG]Adjusting the bolding past 40% produces almost no visible change on the page.
* [FIXED]The bolding progress bar isn't a real progress bar — it doesn't reflect actual progress.
* [FIXED/FOR/MEDICAL/TERM]Word simplification doesn't work very well overall.It performs poorly on scientific terminology and abbreviations.God why we need a capitalisation filter
（honestly I won't assume anyone reading serious papers with Bonita,since there's a reason that scientific terminologies are terminologies--some of them can't be explained easily.）
* [FIXED]Word simplification underlines a word optimistically and only removes the underline on hover when no definition turns up — the line vanishing under the cursor feels like a tease. Now pre-fetches dictionary content and only underlines words it can actually define.
* [FIXED]Add a 'loading' sign for when latency is detected. Spinner now shows in the dock while phrase bolding / word simplification run their async passes, and clears when they settle.
* Popup pre-selects every feature on first launch. 
* POPUP FUNCTIONALITY DOES NOTHING. None of the popup window's toggles currently have any actual effect on the page.
* Let users hide the little floating ball.
* Fold Arial into the Default option, e.g. show it as "Default (Arial)" instead of a separate Arial choice, since the two are basically the same thing.
* Add a 'report issue' button like a small form popup. We're still early in development, so getting this feedback loop in would help.
* For some sites,bolding and sentence splitting do not work.
